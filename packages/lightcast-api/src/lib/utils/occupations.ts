/* eslint-disable @typescript-eslint/ban-types */
/* eslint-disable @typescript-eslint/no-unused-vars */
import { LightcastAPIClient } from "../..";

export default (client: LightcastAPIClient) => ({
  fromSkills: (skills: string[], options?: {}) => {
    return;
  },
});
